/**
 * Spring {@code @Configuration} classes and bean definitions. Keep declarations here;
 * keep behaviour in feature packages.
 */
@NullMarked
package com.onemount.archetype.config;

import org.jspecify.annotations.NullMarked;
