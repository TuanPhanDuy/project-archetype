/**
 * Cross-cutting building blocks shared by every feature: error handling, pagination
 * helpers, base types. Keep business logic out of here — it belongs in feature packages.
 */
@NullMarked
package com.anbit.archetype.common;

import org.jspecify.annotations.NullMarked;
